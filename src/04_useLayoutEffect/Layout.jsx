import React from 'react'

export const Layout = () => {
  return (
    <>
        <h2>useLayoutEffect</h2>
        <p>useLayoutEffect is synchronous and executes the effect before the element is rendered. It should be used carefully because its execution blocks the element render.</p>
    </>
  )
}
